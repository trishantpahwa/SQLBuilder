class Joins:
  inner_joins = 'INNER JOIN '
  outer_joins = 'OUTER JOIN '
  left_join = 'LEFT JOIN '
  right_join = 'RIGHT JOIN '
  full_join = 'FULL JOIN '
  self_join = 'SELF JOIN '
  cartesian_join = 'CARTESIAN JOIN '
